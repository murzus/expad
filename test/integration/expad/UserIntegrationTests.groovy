package expad

import static org.junit.Assert.*
import org.junit.*

class UserIntegrationTests {

    @Before
    void setUp() {
    }

    @After
    void tearDown() {
     	 
	   
    }

    @Test
    void testSomething() {
    	/*  def user= new User(login:'admin',password:'mas')
	      assertNotNull user.save(flush:true)
		  
		 def foundUser = User.get(user.id)
		 foundUser.delete()
         
		//assertFalse User.exists(foundUser.id)*/
		
    }
}
